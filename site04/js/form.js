jQuery(function() {
  var wp_temp_uri = tmp_path.temp_uri;
  var wp_home_url = tmp_path.home_url;

  jQuery('.contact-check.privacy .mwform-checkbox-field-text').html(`<a href="${tmp_path.home_url}/privacy" target="_blank">プライバシーポリシー</a>に同意する`);

})

jQuery(window).scroll(function() {

})

jQuery(window).on('load', function() {

})